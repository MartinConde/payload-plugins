'use client'

/* Client surface. Both shells are Node-safe (they lazy-load the heavy editor /
   canvas), referenced DIRECTLY by the collection config — this barrel exists
   mainly for completeness and external reuse. */

export { PrintAreaInput } from '../ui/PrintAreaInput.js'
export { DesignerField } from '../ui/designer/DesignerField.js'
