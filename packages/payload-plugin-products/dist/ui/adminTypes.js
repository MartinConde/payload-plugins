/* Type-only re-export of the doc-form override contract. Routed through
   `payload-plugin-shadcn-ui` so this surface has zero runtime coupling to the
   admin plugin — the products plugin can install in a vanilla Payload project
   without `payload-plugin-shadcn-admin`. Fully erased by SWC / tsc. */ export { };
