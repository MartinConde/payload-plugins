export { productsPlugin } from './plugin.js';
// Node-safe print-area model + pure helpers + presets, shared with the frontend.
export { normalizePrintAreasValue, newPrintArea, aspectOf, aspectFromDims, placementFromPrintArea, printAreaFromPlacement, normalizePlacement, normalizePlacements, A_SERIES_PRESETS } from './ui/printArea.js';
