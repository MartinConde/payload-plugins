import * as React from 'react';
type LoginFormProps = {
    beforeLogin?: React.ReactNode;
    afterLogin?: React.ReactNode;
};
export declare function LoginForm({ beforeLogin, afterLogin }: LoginFormProps): React.JSX.Element;
export {};
