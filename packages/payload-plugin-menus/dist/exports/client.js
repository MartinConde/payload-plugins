'use client';
/* Client surface. `MenuTreeInput` is the Node-safe `.input` override shell; it
   is referenced DIRECTLY by the collection config (not via importMap), so this
   barrel exists mainly for completeness / external reuse. */ export { MenuTreeInput } from '../ui/MenuTreeInput.js';
